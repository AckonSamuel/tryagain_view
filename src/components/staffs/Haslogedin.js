import React from "react";
import { useSelector } from 'react-redux';

const Haslogedin = () => {
    const staff = useSelector(state => state.staff.staff);

    return (

        <div></div>
    )
};

export default Haslogedin;

